{
  "name": "sublink-plus",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "vitest",
    "build": "vite build",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.8",
    "vitest": "^3.0.9",
    "wrangler": "^3.101.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "sublink-plus": "npm:@hlink9/sublink-plus@^0.1.20"
  }
}
